server {
    listen 80;
    server_name portainer.${NGINX_HOST};

    location / {
        proxy_pass         "https://portainer:9443";
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}